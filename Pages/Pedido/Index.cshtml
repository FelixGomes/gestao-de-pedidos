@page
@model gestao_pedidos.Pages_Pedido.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Pedido[0].ClienteId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pedido[0].ValorTotal)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pedido[0].DataEntrega)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pedido[0].FormaDePagamento)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Pedido) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ClienteId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ValorTotal)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataEntrega)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FormaDePagamento)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.PedidoId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.PedidoId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.PedidoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
